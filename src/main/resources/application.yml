server:
  port: 9090

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/ikprojedb
    username: postgres
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
  mail:
    host: localhost
    port: 25
    username: ''
    password: ''
    protocol: smtp